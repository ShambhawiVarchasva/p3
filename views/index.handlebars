<h5>Articles of {{user.name}}</h5>
<ul class="list-group">
  

  {{#each articles}}
      <li class="list-group-item">
     
        <a href="/article/"+articles._id> {{this.title}}</a></li>
       
      {{/each}}
        </ul>
      